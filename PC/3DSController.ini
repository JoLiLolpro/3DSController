Default port is 8889, if you change this, you must change it in the 3DS's 3DSController.ini as well,

The default smoothing value is 0.3. If you set it to 1, the mouse will follow the raw input exactly, with no smoothing.
The closer you set it to 0, the smoother the mouse movement becomes — but with increased input delay.

Make sure to use a single space, not a tab for seperating settings,


Port: 8889

Smooth: 0.3